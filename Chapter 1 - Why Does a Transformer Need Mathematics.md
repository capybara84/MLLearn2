# 第1章 なぜTransformerに数学が必要なのか

**この章のゴール**

Transformerの数式を「難しい記号の集まり」ではなく、ベクトル・行列・softmax・重み付き和の組み合わせとして見られるようになること。

## 1.1 Transformerは巨大な数式ではなく、ベクトル計算の積み重ねである

『Attention Is All You Need』を初めて読むと、Transformerはとても難しい数学でできているように見えるかもしれません。

たとえば、論文には次のような式が出てきます。

```text
Attention(Q, K, V) = softmax(QK^T / sqrt(d_k))V
```

ぱっと見ると、いかにも数学っぽく見えます。

しかし、Transformerを実装するために必要な見方は、そこまで難しくありません。

Transformerの中で行われていることは、大きく言えば次のような計算の繰り返しです。

```text
ベクトルを作る
行列を掛ける
内積を計算する
softmaxで重みに変える
重みに応じてベクトルを混ぜる
```

つまりTransformerは、巨大で謎めいた数式の塊ではなく、たくさんのベクトル計算と行列計算を積み重ねたものです。

重要なのは、数式を見たときに、次のことを読めるようになることです。

```text
これは何を入力にしているのか
これは何を出力しているのか
shapeはどう変わるのか
この計算は何のためにあるのか
```

数学を専門的に学ぶ必要はありません。

行列の固有値や線形空間の厳密な定義を知らなくても、小さなTransformerを実装することはできます。

微分の厳密な証明を知らなくても、勾配降下法やバックプロパゲーションの大まかな意味を理解することはできます。

確率論を深く知らなくても、softmaxの出力を「次のトークンの確率分布」として扱うことはできます。

この教科書で目指すのは、数学者になることではありません。

目指すのは、Transformerの数式を見たときに、次のように読めるようになることです。

```text
ああ、これはトークン同士の相性を計算しているんだな
これは重みを作っているんだな
これは複数のベクトルを混ぜているんだな
これは損失を小さくするために使うんだな
```

## 1.2 論文の数式を読むために必要な数学

『Attention Is All You Need』の中心にある数式は、Attentionの式です。

```text
Attention(Q, K, V) = softmax(QK^T / sqrt(d_k))V
```

この式を読むために必要な数学は、主に次の6つです。

```text
ベクトル
行列
内積
softmax
平方根
shape
```

ひとつずつ見ると、それほど難しいものではありません。

まず、ベクトルです。

Transformerでは、単語やトークンをそのまま扱うのではなく、数値の並びとして扱います。

たとえば「犬」という単語を、次のような数値の並びで表すとします。

```text
[0.12, -0.44, 0.87, 0.03]
```

このような数値の並びがベクトルです。

実際のTransformerでは、4個ではなく、数百個から数千個の数値を使うことが多いです。

次に、行列です。

行列は、ベクトルを変換するために使います。

Transformerでは、入力されたベクトルから、Query、Key、Valueという3種類のベクトルを作ります。

そのときに行列を掛けます。

```text
入力ベクトル → Queryベクトル
入力ベクトル → Keyベクトル
入力ベクトル → Valueベクトル
```

この変換を、実装では線形層として書くことが多いです。

次に、内積です。

内積は、2つのベクトルがどれくらい似ているか、あるいは相性がよいかを測る計算として使えます。

Attentionでは、QueryとKeyの内積を計算します。

これは、ざっくり言えば、次のことを計算しています。

```text
このトークンは、あのトークンにどれくらい注目すべきか
```

次に、softmaxです。

QueryとKeyの内積を計算すると、トークン同士の相性スコアが出ます。

ただし、そのスコアはそのままでは扱いにくいので、softmaxによって「合計が1になる重み」に変換します。

たとえば、次のようなスコアがあったとします。

```text
[2.0, 1.0, 0.1]
```

softmaxをかけると、次のような重みに変換されます。

```text
[0.66, 0.24, 0.10]
```

この重みは、次のような意味として解釈できます。

```text
1番目のトークンを強く見る
2番目のトークンを少し見る
3番目のトークンはあまり見ない
```

次に、平方根です。

Attentionの式には、`sqrt(d_k)` で割る部分があります。

```text
QK^T / sqrt(d_k)
```

これは、内積の値が大きくなりすぎるのを防ぐための調整です。

今の段階では、厳密な理由を完全に理解しなくてもかまいません。

まずは、次のように理解しておけば十分です。

```text
softmaxに入れる前に、スコアの大きさを調整している
```

最後に、shapeです。

Transformerを実装するとき、もっとも重要な感覚のひとつがshapeです。

shapeとは、テンソルの形のことです。

たとえば、次のようなものです。

```text
ベクトル: [4]
行列: [3, 4]
バッチ付きのトークン列: [batch_size, seq_len, d_model]
```

Transformerの実装では、次のようなことを追える必要があります。

```text
このテンソルのshapeは何か
行列積の結果のshapeはどうなるか
softmaxはどの次元にかけるのか
Multi-Head Attentionでどうreshapeするのか
```

実装で詰まる原因の多くは、数学の高度な理解不足ではなく、shapeの理解不足です。

そのため、この教科書では「数式の意味」と同じくらい「shapeを読むこと」を重視します。

## 1.3 実装で本当に必要になる数学

Transformerを実装するために必要な数学は、大学数学を広く学ぶことではありません。

必要なのは、実装中に出てくる計算を理解することです。

たとえば、Transformerを実装していると、次のような処理が出てきます。

```python
q = self.w_q(x)
k = self.w_k(x)
v = self.w_v(x)

scores = q @ k.transpose(-2, -1)
scores = scores / math.sqrt(d_k)
weights = torch.softmax(scores, dim=-1)
out = weights @ v
```

このコードを理解するために必要なのは、次のような知識です。

```text
線形層は何をしているのか
行列積 `@` は何をしているのか
転置とは何か
なぜ `k` を転置するのか
なぜ `sqrt(d_k)` で割るのか
softmaxは何をしているのか
`weights @ v` は何をしているのか
```

つまり、実装で必要な数学は、コードと直接対応しています。

数学を学ぶときも、次のように考えるとよいです。

```text
この数学は、どのコードに対応しているのか
この数式は、どの処理を説明しているのか
この概念は、Transformerのどこで使われるのか
```

たとえば、内積を学ぶ目的は、内積そのものに詳しくなることではありません。

目的は、次のことを理解することです。

```text
Attentionでは、QueryとKeyの内積でトークン同士の相性を計算している
```

行列積を学ぶ目的は、行列計算の理論を極めることではありません。

目的は、次のことを理解することです。

```text
複数のトークンをまとめて一度に変換するために、行列積を使っている
```

微分を学ぶ目的は、微積分の問題を解けるようになることではありません。

目的は、次のことを理解することです。

```text
損失を小さくするために、各パラメータをどちら向きに動かせばよいかを知る
```

確率分布を学ぶ目的は、確率論を体系的に理解することではありません。

目的は、次のことを理解することです。

```text
言語モデルは、次のトークン候補それぞれに確率を割り当てている
```

このように、実装から逆算して数学を学ぶのが、この教科書の方針です。

## 1.4 証明よりも「形」と「意味」を重視する

数学の教科書では、定義、定理、証明が重視されます。

それは数学を専門的に学ぶ上では重要です。

しかし、Transformerを実装するための数学では、最初から証明を重視しすぎると遠回りになります。

たとえば、行列積について、数学的に厳密な定義を学ぶことはできます。

しかし、Transformer実装においてまず必要なのは、次の感覚です。

```text
[seq_len, d_model] の入力に
[d_model, d_k] の重みを掛けると
[seq_len, d_k] の出力になる
```

つまり、まずはshapeが追えることが大事です。

softmaxについても同じです。

softmaxの性質を厳密に証明するより、まずは次のことがわかる方が大事です。

```text
softmaxは、スコアの列を合計1の重みに変換する
大きいスコアほど大きい重みになる
Attentionでは、どのトークンを見るかを決めるために使う
```

微分についても同じです。

極限の定義から厳密に理解するより、まずは次のことがわかる方が大事です。

```text
微分は、少し動かしたときに値がどう変わるかを見る道具である
勾配は、損失を増やす方向を教えてくれる
勾配と逆向きに動けば、損失を下げられる
```

もちろん、厳密な理解が不要という意味ではありません。

ただし、学ぶ順番としては、次の流れがよいです。

```text
まず意味を理解する
次にshapeを追う
次にコードで動かす
最後に必要なら厳密な定義に戻る
```

特に、Transformerを自分で実装したい場合は、数学の理解をコードに接続することが重要です。

数学を「紙の上の記号」として読むだけではなく、次のことを常に考える必要があります。

```text
この式はPythonでどう書くのか
このテンソルのshapeは何か
この計算結果は何を表すのか
```

この教科書では、数式を出すときも、できるだけ次の3つをセットで説明します。

```text
数式
意味
実装上のshape
```

たとえばAttentionの式であれば、次のように読みます。

```text
Attention(Q, K, V) = softmax(QK^T / sqrt(d_k))V
```

これは数式としては短いですが、中身は次の処理です。

```text
QとKの内積で、トークン同士の相性を計算する
sqrt(d_k)で割って、スコアの大きさを調整する
softmaxで、相性スコアを重みに変換する
その重みに応じてVを混ぜる
```

さらにshapeで見ると、たとえば次のようになります。

```text
Q: [seq_len, d_k]
K: [seq_len, d_k]
V: [seq_len, d_v]

QK^T: [seq_len, seq_len]

softmax(QK^T / sqrt(d_k)): [seq_len, seq_len]

softmax(QK^T / sqrt(d_k))V: [seq_len, d_v]
```

このように、数式を「意味」と「shape」に分解して読めるようになることが、この教科書の大きな目標です。

## 1.5 この教科書で扱う範囲

この教科書で扱う範囲は、Transformerを実装するための最低限の数学です。

具体的には、次の内容を扱います。

```text
スカラー
ベクトル
行列
テンソル
shape
内積
行列積
転置
線形変換
softmax
確率分布
cross entropy
微分
偏微分
勾配
勾配降下法
連鎖律
正規化
Attentionの数式
```

これらは、Transformerを理解する上で何度も出てきます。

たとえば、ベクトルと行列は、embeddingや線形層で使います。

```text
token → embedding vector
embedding vector → Q/K/V
```

内積は、Attentionで使います。

```text
QueryとKeyの相性を計算する
```

softmaxは、Attention weightや次トークン予測で使います。

```text
スコアを確率のような重みに変換する
```

確率分布は、言語モデルの出力を理解するために使います。

```text
次のトークンが何であるかの確率分布を出す
```

cross entropyは、学習時の損失関数として使います。

```text
正解トークンの確率が高くなるように学習する
```

微分と勾配は、モデルのパラメータを更新するために使います。

```text
損失が小さくなる方向に重みを動かす
```

正規化は、Layer Normalizationを理解するために使います。

```text
値のスケールを整えて、学習を安定させる
```

この教科書では、それぞれの数学を単独で説明するのではなく、Transformerとの関係を意識しながら説明します。

たとえば、内積を説明するときは、単に計算方法だけでなく、次のことまで説明します。

```text
なぜAttentionで内積を使うのか
QueryとKeyの内積は何を意味するのか
```

softmaxを説明するときは、単に公式を示すだけでなく、次のことまで説明します。

```text
なぜAttention weightにsoftmaxを使うのか
なぜ出力が重みとして解釈できるのか
```

微分を説明するときは、計算問題をたくさん解くのではなく、次の理解につなげます。

```text
ニューラルネットワークはどうやって学習するのか
損失を小さくするとはどういうことか
```

この教科書を読み終えたときに目指す状態は、次のようなものです。

```text
Transformerの数式を見て、何をしているか大まかに説明できる
PyTorchのテンソル計算でshapeを追える
Attentionの式を日本語で説明できる
softmaxやcross entropyがどこで使われるかわかる
勾配降下法によってモデルが学習する流れを説明できる
```

この状態になれば、次の「ニューラルネットワークの基本」に進む準備ができています。

## 1.6 この教科書で扱わない範囲

この教科書は、数学全体を体系的に学ぶための本ではありません。

そのため、あえて扱わない内容もあります。

たとえば、次のような内容は扱いません。

```text
線形代数の厳密な公理
ベクトル空間の抽象的な定義
固有値・固有ベクトルの詳細
行列式の理論
逆行列の高度な議論
多変数微分の厳密な証明
測度論的な確率論
統計学の体系的な理論
最適化理論の詳細
```

これらは重要な数学です。

しかし、最初にTransformerを理解し、自分で小さく実装するためには、必須ではありません。

もちろん、将来的に深く学ぶ価値はあります。

たとえば、最適化理論を学ぶと、Adamなどのoptimizerをより深く理解できます。

確率論や統計学を学ぶと、言語モデルの出力や学習の性質をより深く理解できます。

線形代数を深く学ぶと、embedding空間や表現学習についてより高度に考えられるようになります。

しかし、最初からそこまで広げると、Transformerにたどり着く前に学習範囲が広がりすぎます。

この教科書では、目的を絞ります。

目的は、次の式を読めるようになることです。

```text
Attention(Q, K, V) = softmax(QK^T / sqrt(d_k))V
```

そして、この式をコードに落とせるようになることです。

さらに、この式がTransformer全体の中でどのような役割を持っているのかを理解することです。

そのため、この教科書では「使う数学」を優先します。

「数学として美しい体系」よりも、「Transformerを理解するために必要な道具」として数学を扱います。

## 1.7 この章のまとめ

この章では、Transformerを理解するために、なぜ数学が必要なのかを説明しました。

重要なのは、Transformerに必要な数学は、最初から高度な数学ではないということです。

必要なのは、次のような基礎です。

```text
ベクトル
行列
内積
softmax
確率分布
微分
勾配
shape
```

Transformerの数式は、一見すると難しそうに見えます。

しかし、実際には、次のような処理の組み合わせです。

```text
ベクトルを変換する
トークン同士の相性を計算する
softmaxで重みにする
重みに応じて情報を混ぜる
損失を計算する
勾配でパラメータを更新する
```

この教科書では、数学を厳密な証明中心ではなく、意味、shape、実装との対応を中心に学びます。

特に大事なのは、数式を見たときに、次のことを考えることです。

```text
これは何を入力にしているのか
これは何を出力しているのか
shapeはどう変わるのか
この計算はTransformerのどこで使われるのか
```

次章では、まずもっとも基本になる「スカラー」「ベクトル」「行列」「テンソル」について学びます。

Transformerでは、すべてのデータが最終的にテンソルとして扱われます。

そのため、次章で学ぶshapeの感覚は、今後のすべての章の土台になります。
